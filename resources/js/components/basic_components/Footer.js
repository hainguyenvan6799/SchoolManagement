import React, {Component} from 'react';
import ReactDOM from 'react-dom';

export default class Footer extends Component{
    render(){
        return (
            <div className="footer bg-success bg-gradient">
                <h2 className="text-center text-danger">Footer</h2>
            </div>
        )
    }
}