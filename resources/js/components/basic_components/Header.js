import React, {Component} from 'react';
import ReactDOM from 'react-dom';

export default class Header extends Component{
    render(){
        return (
            <div className="header bg-warning bg-gradient">
                <h2 className="text-center text-danger">Header</h2>
            </div>
        )
    }
}